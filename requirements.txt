numpy>=1.24
jsonschema>=4.0
TA-Lib>=0.6           # 지표 계산(검증된 C 구현). ★ 먼저 C 라이브러리 설치 필요:
                      #   macOS:  brew install ta-lib
                      #   Ubuntu: apt-get install ta-lib  (또는 소스 빌드)
                      # 그 후: pip install TA-Lib
# pandas 불필요 (numpy + TA-Lib). 실데이터 어댑터 추가 시 mysql 커넥터 등 별도.
